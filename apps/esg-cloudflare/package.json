{
  "name": "corp-esg-llmwiki-cloudflare-p0",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787 --ip 127.0.0.1",
    "dry-run": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0"
  },
  "devDependencies": {
    "wrangler": "4.132.0"
  }
}
